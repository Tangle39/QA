ppio  
第一题：
使用hash数据结构来存储各个钱包地址的余额信息，键名为Rank，Address，Balance，Txcount。方法有HMSET进行设置，HGET key field来获取指定字段的值

第二题:
设置10个不同range的请求
10 get /demo/release/macos/latest/ppio-demo.dmg

第三题：
#!/bin/sh

proc_name="/usr/local/bin/ppio-demo"  #进程名

file_name="/usr/local/bin/ppio-demo"
pid=0
proc_num() 
{
  num=`ps -ef | grep $proc_name | grep -v grep | wc -l` 
  return $num 

}
proc_id()
{  
    pid=`ps -ef | grep $proc_name | grep -v grep | awk '{print $2}'`  
} 

proc_num
number=$?  
if [ $number -eq 0 ]                                    # 判断进程是否存在  
then   
    /usr/local/bin/ppio-demo start    # 重启进程的命令
    proc_id                                         # 获取新进程号  
    echo ${pid}, `date` >> $file_name      # 将新进程号和重启时间记录  
fi  

第四题：
1.
CREATE TABLE `user` (  
`nickname` varchar(32) NOT NULL,  
`follow_num` int(11) NOT NULL,  
`follower_num` int(11) NOT NULL,  
`weibo_num` int(11) NOT NULL,  
PRIMARY KEY (`nickname`);  

CREATE TABLE `macroblog` (  
`nickname` varchar(32) NOT NULL,  
`content` TEXT NOT NULL,  
`post_time` date NOT NULL,
`repost_num` int(11) NOT NULL, 
`comment_num` int(11) NOT NULL, 
`like_num` int(11) NOT NULL,  
;  

2.count(like_num) from macblog where nickname = PPlabs2019 as t

3.select nickname,follow_num,follower_num from user where nickename = （select nickname from macblog limit 0,50 order by t desc)

4. 使用count distinct提升效率/建立nickname和点赞数t的索引来加快查询速度
